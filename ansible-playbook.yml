- hosts: all
  become: yes
  tasks:
    - name: Install httpd service
      apt:
        name: apache2
        state: present
        update_cache: yes

    - name: Start httpd service
      service:
        name: apache2
        state: started
        enabled: true
